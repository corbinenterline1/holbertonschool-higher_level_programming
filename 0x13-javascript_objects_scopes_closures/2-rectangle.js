#!/usr/bin/node
// Creates class Rectangle with w & h args, with value check on args
class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
}
module.exports = Rectangle;
